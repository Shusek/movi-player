/** Public API contract version. Increment only for incompatible contract changes. */
export const MOVI_API_VERSION = 1;

/** Upstream package version. Fork release builds replace this in their release commit. */
export const MOVI_VERSION = "0.3.5";
